<!-- memory-root: ~/memory/projects/x-tweet-export -->

# x-tweet-export

A Chrome extension that exports any X (Twitter) user's tweets to CSV/JSON with one click. Zero configuration — works because the user is already logged into X in their browser.

## Project Docs

- **Design Doc**: `~/memory/projects/x-tweet-export/docs/design.md`
